import React from 'react';
import styles from "./Skills.module.scss"

export const Skills = () => {
    return (
        <div className={styles.skillsBlock}>
            <p>Block Skills</p>
        </div>
    );
};